# 乌东文旅社区板块功能文档

> **更新时间**: 2026-09-11  
> **文档版本**: v1.0  
> **入口地址**: http://localhost:5173/community

---

## 📋 文档概述

本文档详细规定了乌东文旅平台社区板块（Community）的完整功能需求、数据结构、API 接口、前端实现和用户交互流程。社区板块是用户分享游记、发现内容、互动交流的核心功能区域。

---

## 🎯 功能需求总览

### 一级功能清单

| 序号 | 功能模块 | 描述 | 路由 | 认证需求 |
|------|--------|------|------|--------|
| 1 | 社区首页 | 平台概览、热门话题、热门游记 | `/community` | 否 |
| 2 | 浏览游记 | 游记列表、分类、筛选、搜索 | `/community/feed` | 否 |
| 3 | 游记详情 | 完整游记、评论、点赞、收藏 | `/community/:id` | 否 |
| 4 | 发布游记 | 创建新游记、上传图片、选择话题 | `/community/publish` | 是 |
| 5 | 热门话题 | 话题列表、话题详情、话题过滤 | `/community/topics` | 否 |
| 6 | 用户主页 | 用户信息、作品、收藏、关注 | `/user/:id` | 否 |
| 7 | 搜索功能 | 全局搜索游记、话题、用户 | `/community/search` | 否 |

---

## 🌟 功能详细需求

### 模块 1：浏览游记功能

#### 1.1 游记列表页面 (`/community/feed`)

**页面布局**：
- 顶部：导航栏 + 搜索框
- 左侧/顶部：话题分类标签（可过滤）
- 中央：游记卡片列表（网格/列表切换）
- 右侧：热门推荐侧边栏（可选）

**功能特性**：

```
✓ 游记列表展示
  - 标题、封面图、作者名、发布时间
  - 点赞数、收藏数、评论数、浏览数
  - 话题标签（可点击进入话题页）
  - 位置信息（若有）
  - 内容摘要（截断至 150 字）

✓ 排序与筛选
  - 排序方式：最新 / 热门 / 评论数 / 浏览数
  - 话题过滤：支持单选或多选话题
  - 时间范围：今天 / 本周 / 本月 / 全部
  - 作者关注过滤：仅显示我关注的作者（需登录）

✓ 分页加载
  - 默认每页 10 条
  - 分页器或无限滚动（二选一）
  - 加载状态提示

✓ 搜索功能
  - 实时搜索游记标题和内容
  - 搜索结果高亮显示
```

**关键交互**：
- 点击游记卡片 → 进入游记详情页面
- 点击作者名 → 进入作者主页
- 点击话题标签 → 过滤该话题的游记
- 点击"关注"按钮 → 关注作者（需登录）

---

#### 1.2 游记详情页面 (`/community/:id`)

**页面布局**：
```
┌─ 导航栏 ─────────────────────────────────┐
├─ 游记内容区 ──────────────┬─ 侧边栏 ─────┤
│ 标题                      │ 作者信息卡   │
│ 作者 / 发布时间 / 位置    │ 关注按钮    │
│ 图片轮播                  │ 话题列表    │
│ 正文内容                  │ 相关推荐    │
│ 话题标签                  │             │
│ 交互区（点赞/收藏/分享）  │             │
│ 评论区                    │             │
└─────────────────────────────────────────┘
```

**内容展示**：

```
【标题】
大标题，字体加粗，28px 以上

【发布信息】
- 作者头像 + 昵称（可点击进入主页）
- 发布时间（相对时间，如"2 小时前"）
- 位置信息（若有地址信息则显示）
- 浏览数 / 点赞数 / 评论数

【图片展示】
- 支持多图轮播（1-9 张图片）
- 图片支持点击放大查看
- 支持左右滑动切换（移动端）
- 图片加载动画

【视频展示】（可选）
- 若有视频 URL，显示视频播放器
- 支持暂停、全屏、字幕等基本操作

【正文内容】
- 最大 5000 字
- 支持换行、制表符等基础格式
- 外链自动识别并链接化

【话题标签】
- 显示该游记关联的所有话题
- 话题名前加 # 符号
- 支持点击跳转到话题页面
```

**交互功能**：

```
【点赞功能】
- 显示当前点赞数
- 登录后点击心形图标可点赞/取消点赞
- 点赞状态实时反馈（心形变红）
- 未登录提示去登录

【收藏功能】
- 显示当前收藏数
- 登录后点击星形图标可收藏/取消收藏
- 收藏状态实时反馈（星形变满）
- 未登录提示去登录

【分享功能】
- 分享到微信（生成二维码分享链接）
- 复制链接
- 分享到微博/QQ（可选）

【举报功能】
- 三点菜单中提供"举报"选项
- 举报对话框：选择举报原因 + 描述
- 原因包括：虚假内容、广告、色情、暴力等
- 举报后提示"已提交举报，感谢您的反馈"
```

---

#### 1.3 评论系统

**评论区设计**：

```
┌─ 评论区 ────────────────────────────────┐
│ 评论数：999 条                          │
├─────────────────────────────────────────┤
│ 【登录后显示输入框】                      │
│ ├─ 用户头像                             │
│ ├─ 输入框："说说你的感受..."             │
│ ├─ 发送按钮                             │
│                                         │
│ 【一级评论列表】                          │
│ ├─ 评论项 #1                            │
│ │  ├─ 用户头像 + 昵称（可点击进主页）    │
│ │  ├─ 评论时间（相对时间）              │
│ │  ├─ 评论内容（可换行，最多 500 字）   │
│ │  ├─ 交互按钮：👍点赞 💬回复 更多      │
│ │  ├─ 点赞数显示                        │
│ │  │                                    │
│ │  └─ 【二级评论（展开/收起）】         │
│ │     ├─ 回复项 #1.1                   │
│ │     │  ├─ 用户头像 + 昵称            │
│ │     │  ├─ @被回复用户 评论内容       │
│ │     │  ├─ 交互按钮：👍点赞 💬回复   │
│ │     │                                │
│ │     ├─ 回复项 #1.2                   │
│ │     └─ ...                           │
│ │                                      │
│ ├─ 评论项 #2                            │
│ └─ ...                                  │
│                                         │
│ 【分页/加载更多】                        │
│ └─ "加载更多评论" 按钮                   │
└─────────────────────────────────────────┘
```

**评论功能详解**：

```
【评论发表】
✓ 登录用户可发表评论
✓ 一级评论：直接评论游记
✓ 二级评论（回复）：通过点击"回复"按钮，在输入框中自动 @被回复用户
✓ 评论字数限制：1-500 字
✓ 发送后评论立即显示在顶部（乐观 UI）
✓ 敏感词过滤：包含敏感词提示"包含不适合内容"
✓ 未登录提示：点击评论框 → 跳转登录

【评论展示结构】
一级评论（回复游记的用户）
  ├─ 用户头像 + 昵称
  ├─ 发布时间（2 小时前）
  ├─ 评论内容（最多 3 行，超出展开）
  ├─ 点赞数 / 回复按钮 / 更多菜单
  │
  └─ 二级评论（回复一级评论的用户）
     ├─ 用户头像 + 昵称 + "@一级评论者"
     ├─ 发布时间
     ├─ 评论内容
     └─ 点赞数 / 回复按钮 / 更多菜单

【默认展示数量】
✓ 一级评论：显示 3 条，有"查看全部 N 条评论"按钮
✓ 二级评论：一级评论下显示 2 条回复，有"查看全部 N 条回复"

【评论点赞】
✓ 点击点赞图标：心形变红，数字加 1
✓ 再次点击：取消点赞，数字减 1
✓ 未登录提示去登录

【删除评论】
✓ 评论作者可删除自己的评论
✓ 游记作者可删除他人的不当评论（需要权限）
✓ 平台管理员可删除任何评论
✓ 删除后评论从列表消失

【评论排序】
✓ 默认按发布时间倒序（最新在前）
✓ 可选排序：最新 / 最赞 / 最早
```

---

#### 1.4 作者信息侧边栏

**侧边栏内容**：

```
┌─ 作者信息卡 ──────────────┐
│                           │
│  👤 用户头像（大，90px）   │
│                           │
│  昵称                     │
│  @username               │
│                           │
│  个人简介（最多 100 字）   │
│  地区：贵州               │
│                           │
│  粉丝数：999              │
│  关注数：99               │
│  获赞数：9.9K             │
│                           │
│  ┌─ 按钮组 ───────────┐  │
│  │ [关注] [私信] [更多] │  │
│  └─────────────────┘  │
│                           │
├─ 话题标签 ────────────────┤
│ #苗寨风光 #非遗手作       │
│ #苗家美食 #旅拍攻略       │
│                           │
├─ 相关推荐 ────────────────┤
│ 这位作者的其他游记：       │
│ • 乌东苗寨梯田风光        │
│ • 长桌宴美食体验          │
│ • 民宿民族风体验          │
│                           │
└───────────────────────────┘
```

**侧边栏功能**：
- 点击头像/昵称 → 进入作者主页
- "关注"按钮 → 关注/取消关注该作者
- "私信"按钮 → 跳转到私信页面（需登录）
- "更多" → 举报、拉黑等操作
- 话题标签 → 点击过滤该话题的游记
- 相关推荐 → 点击进入该游记

---

### 模块 2：游记详情 - 用户主页链接

#### 2.1 点击作者名进入用户主页

**用户主页路由**：`/user/:userId`

**用户主页布局**：

```
┌─ 顶部用户信息区 ──────────────────────────┐
│  背景图（可选）                           │
│  ┌──────────────────────────────┐       │
│  │ 👤 用户头像                   │ [编辑资料] │
│  │ 昵称                          │ [关注]    │
│  │ @username                    │ [私信]    │
│  │ 个人简介 / 地区 / 加入时间    │ [更多]    │
│  │ 粉丝数 / 关注数 / 获赞数      │          │
│  └──────────────────────────────┘       │
├─ 标签页导航 ──────────────────────────────┤
│ [发布的游记] [点赞的游记] [收藏的游记]    │
│ [关注的作者] [关注的话题]                │
├─────────────────────────────────────────┤
│ 【当前标签页的内容】                      │
│ ...                                     │
└─────────────────────────────────────────┘
```

#### 2.2 用户主页的五个标签页

**标签页 1：发布的游记**

```
【显示该用户发布的所有游记】

✓ 时间倒序排列（最新发布在前）
✓ 网格布局（3 列或 4 列）
✓ 每个游记卡片显示：
  - 封面图（第一张图片）
  - 标题
  - 发布时间
  - 浏览/点赞/评论数
✓ 游记数统计："共 X 篇游记"
✓ 分页展示（每页 12 条）
✓ 空状态提示："该用户还没有发布游记"
```

**标签页 2：点赞的游记**

```
【显示该用户点过赞的所有游记】

✓ 点赞时间倒序排列（最近点赞的在前）
✓ 列表布局（与游记列表一致）
✓ 每个游记卡片显示：
  - 作者信息
  - 游记标题、摘要
  - 发布时间
  - 互动数据
✓ 点赞数统计："共 X 篇被点赞的游记"
✓ 分页展示
✓ 空状态提示："该用户还没有点赞过游记"
✓ 仅自己可见本标签页内容（隐私设置）
```

**标签页 3：收藏的游记**

```
【显示该用户收藏的所有游记】

✓ 收藏时间倒序排列
✓ 列表布局
✓ 可按收藏夹分类展示
✓ 收藏数统计："共 X 篇收藏游记"
✓ 分页展示
✓ 空状态提示："该用户还没有收藏游记"
✓ 仅自己可见本标签页内容（隐私设置）
```

**标签页 4：关注的作者**

```
【显示该用户关注的所有作者】

✓ 关注时间倒序排列
✓ 网格布局（4 列或 5 列）或列表布局
✓ 每个作者卡片显示：
  - 头像
  - 昵称
  - 个人简介
  - 粉丝数
  - [已关注] 按钮（当前用户如果也关注了该作者）
✓ 关注作者数统计："关注了 X 位作者"
✓ 分页展示
✓ 空状态提示："该用户还没有关注任何作者"
✓ 点击作者卡片 → 进入该作者主页
```

**标签页 5：关注的话题**

```
【显示该用户关注的所有话题】

✓ 关注时间倒序排列
✓ 列表布局或网格布局
✓ 每个话题卡片显示：
  - 话题名（带 # 符号）
  - 话题描述
  - 讨论数 / 关注数
  - [已关注] 按钮（当前用户如果也关注了该话题）
✓ 关注话题数统计："关注了 X 个话题"
✓ 分页展示
✓ 空状态提示："该用户还没有关注任何话题"
✓ 点击话题 → 进入话题页面
```

---

### 模块 3：发布游记功能

#### 3.1 发布游记页面 (`/community/publish`)

**权限要求**：
- 仅登录用户可访问
- 未登录用户访问 → 重定向到登录页面，登录后返回发布页

**页面布局**：

```
┌─ 导航栏 ─────────────────────────────────────┐
├─ 发布游记表单 ───────────────────────────────┤
│                                             │
│ 【基础信息】                                  │
│ 标题：[输入框，最多 200 字]                   │
│                                             │
│ 【正文内容】                                  │
│ 内容：[文本框，最多 5000 字，显示已用字数]     │
│                                             │
│ 【图片上传】                                  │
│ [图片预览区域]                                │
│ ┌─ 已上传图片 ─┬─ 已上传图片 ─┬─ 上传按钮 ┐ │
│ │ 图 1         │ 图 2         │ +  1/9   │ │
│ │ [✕删除]      │ [✕删除]      │          │ │
│ └──────────────┴──────────────┴──────────┘ │
│                                             │
│ 【话题选择】                                  │
│ 话题：[下拉多选框]                            │
│       ☑ #苗寨风光  ☑ #非遗手作              │
│       ☐ #苗家美食  ☐ #旅拍攻略              │
│                                             │
│ 【位置信息】                                  │
│ 位置：[输入框，如"乌东古城"]                  │
│                                             │
│ 【视频链接】（可选）                          │
│ 视频：[输入框，粘贴视频 URL]                  │
│                                             │
│ 【发布按钮】                                  │
│ [保存草稿] [预览] [发布]                      │
│                                             │
└─────────────────────────────────────────────┘
```

**功能详解**：

```
【标题输入】
✓ 最多 200 字
✓ 实时字数统计：已输入 X/200
✓ 必填项：不填提示"请输入标题"

【正文内容】
✓ 最多 5000 字
✓ 支持换行、缩进
✓ 实时字数统计：已输入 X/5000
✓ 必填项：不填提示"请输入正文内容"
✓ 敏感词提醒：检测敏感词，提示用户

【图片上传】
✓ 支持 JPG / PNG / WebP 格式
✓ 单张图片最大 5MB
✓ 最多 9 张图片
✓ 上传进度条显示
✓ 支持拖拽上传
✓ 支持点击"选择文件"弹出文件对话框
✓ 每张图片可预览
✓ 每张图片右上角有删除按钮（✕）
✓ 图片可拖拽调整顺序（可选）
✓ 必填项：至少上传 1 张图片

【话题选择】
✓ 下拉多选框
✓ 显示系统中已有的所有话题
✓ 话题前显示 # 符号
✓ 可搜索话题名
✓ 支持创建新话题（可选功能）
✓ 最多选择 5 个话题
✓ 可选项：不填表示不绑定话题

【位置信息】
✓ 输入框，支持手工输入
✓ 下拉提示：输入时显示匹配的地点
✓ 常见地点预置：乌东村、乌东苗寨、吊脚楼民宿等
✓ 可选项：不填表示不显示位置

【视频链接】
✓ 支持输入视频 URL（优酷、B 站等）
✓ 验证 URL 格式
✓ 可选项：不填表示不含视频

【发布选项】
✓ 保存草稿：保存当前内容到本地，后续可继续编辑
✓ 预览：打开预览窗口，显示发布效果
✓ 发布：提交到服务器进行内容审核

【发布流程】
1. 点击"发布"按钮
2. 前端验证所有必填项
3. 上传图片到服务器（如未上传）
4. 提交游记信息到 `/api/community/posts` 接口
5. 等待响应：
   ✓ 成功：提示"发布成功，待审核"，跳转到我的游记页
   ✗ 失败：显示错误信息，保留草稿

【内容审核】
✓ 发布后状态为"审核中"
✓ 用户在个人中心可查看审核状态
✓ 审核通过后，游记自动发布
✓ 审核不通过：显示驳回原因，允许重新编辑
```

---

### 模块 4：热门话题功能

#### 4.1 热门话题列表页面 (`/community/topics`)

**页面布局**：

```
┌─ 导航栏 + 搜索 ───────────────────────────┐
├─ 话题列表 ────────────────────────────────┤
│                                          │
│ 搜索框 / 排序选项                        │
│ 排序: [最新 ▼] [热度 ▼] [关注数 ▼]      │
│                                          │
│ 话题卡片 #1                               │
│ ├─ 话题图标 / 封面                        │
│ ├─ 话题名：#苗寨风光                      │
│ ├─ 描述：分享乌东苗寨的梯田、吊脚楼...   │
│ ├─ 统计：999 讨论 · 999 关注              │
│ └─ 按钮：[关注] 或 [已关注]              │
│                                          │
│ 话题卡片 #2                               │
│ ...                                      │
│                                          │
│ 分页 [< 上一页] [1][2][3][4] [下一页 >] │
│                                          │
└───────────────────────────────────────────┘
```

**话题卡片详情**：

```
【话题卡片展示】
- 话题名：带 # 符号，如 #苗寨风光
- 话题描述：文字形式或带图
- 话题图标：可选（emoji 或图片）
- 讨论数：该话题下的游记数量
- 关注数：关注该话题的用户数
- 关注按钮：
  ✓ 未关注状态：显示蓝色 [关注] 按钮
  ✓ 已关注状态：显示灰色 [已关注] 按钮
  ✓ 点击后切换状态，关注/粉丝数实时更新

【话题搜索】
✓ 实时搜索：输入时动态过滤话题
✓ 匹配规则：按话题名称中文或英文模糊匹配
✓ 搜索范围：话题名称、描述

【话题排序】
✓ 最新：按创建时间倒序
✓ 热度：按讨论数和关注数综合排序
✓ 关注数：按关注用户数倒序

【交互流程】
✓ 点击话题卡片 → 进入话题详情页面（显示该话题下的所有游记）
✓ 点击关注按钮 → 关注/取消关注该话题
✓ 点击话题名 → 进入话题详情页面

【分页展示】
✓ 每页显示 10 个话题
✓ 页码导航：上一页 / 页码 / 下一页
✓ 显示"第 X 页，共 Y 页"
```

#### 4.2 话题详情页面

**路由**：`/community/feed?topic=:topicId`

**页面布局**：

```
┌─ 导航栏 ──────────────────────────────────┐
├─ 话题头部 ────────────────────────────────┤
│ 话题名：#苗寨风光                         │
│ 描述：分享乌东苗寨的梯田、吊脚楼...      │
│ 统计：999 篇讨论 · 999 人关注            │
│ [关注] 按钮                               │
│                                          │
├─ 话题相关游记列表 ────────────────────────┤
│                                          │
│ 游记卡片 #1                               │
│ 游记卡片 #2                               │
│ ...                                      │
│                                          │
│ 分页                                      │
│                                          │
└────────────────────────────────────────────┘
```

**话题详情页功能**：

```
【话题头部展示】
✓ 话题名称（带 #）
✓ 话题描述（说明该话题是什么）
✓ 话题统计信息：
  - 讨论数：绑定该话题的游记数
  - 关注数：关注该话题的用户数
✓ 关注按钮：显示当前用户是否已关注该话题

【该话题下的游记列表】
✓ 筛选条件已自动设置为"话题 = 当前话题"
✓ 显示所有绑定该话题的游记
✓ 支持进一步的排序/筛选：
  - 排序：最新 / 热门 / 评论最多
  - 时间范围：今天 / 本周 / 本月 / 全部
✓ 分页展示（每页 10 条）

【相关话题推荐】
✓ 显示 3-5 个相关话题
✓ 可点击跳转到相关话题
```

---

## 🗄️ 数据结构定义

### 游记对象 (Post)

```typescript
interface Post {
  // 基础信息
  id: number;                    // 唯一 ID
  title: string;                 // 标题（≤ 200 字）
  content: string;               // 正文（≤ 5000 字）
  
  // 媒体
  images: string;                // 图片 JSON 数组 ["url1", "url2", ...]（1-9 张）
  videoUrl?: string;             // 视频 URL（可选）
  
  // 位置与话题
  location?: string;             // 位置名称，如"乌东古城"
  topicIds: number[];            // 关联话题 ID 数组
  topics?: Topic[];               // 关联话题对象数组（可选）
  
  // 作者信息
  userId: number;                // 作者 ID
  author: {
    id: number;
    nickname: string;            // 昵称
    avatar?: string;             // 头像 URL
    bio?: string;                // 个人简介
  };
  
  // 统计数据
  viewCount: number;             // 浏览次数
  likeCount: number;             // 点赞数
  commentCount: number;          // 评论数
  favoriteCount: number;         // 收藏数
  
  // 状态
  status: 0 | 1 | 2;             // 0: 审核中, 1: 已发布, 2: 已下架
  rejectReason?: string;         // 驳回原因（status=2 时有值）
  isHot: boolean;                // 是否为热门内容
  isLiked?: boolean;             // 当前用户是否已点赞
  isFavorited?: boolean;         // 当前用户是否已收藏
  
  // 时间戳
  createdAt: string;             // 创建时间（ISO 8601）
  publishedAt: string;           // 发布时间（过审时间）
  updatedAt: string;             // 更新时间
}
```

### 评论对象 (Comment)

```typescript
interface Comment {
  id: number;                    // 唯一 ID
  postId: number;                // 所属游记 ID
  userId: number;                // 评论者 ID
  content: string;               // 评论内容（≤ 500 字）
  
  // 层级结构
  parentId: number;              // 父评论 ID（0 表示一级评论）
  replyUserId?: number;          // 被回复的用户 ID
  replyUser?: {
    id: number;
    nickname: string;
  };
  
  // 作者信息
  author: {
    id: number;
    nickname: string;
    avatar?: string;
  };
  
  // 统计
  likeCount: number;             // 点赞数
  isLiked?: boolean;             // 当前用户是否已点赞
  
  // 状态
  status: 1 | 0;                 // 1: 正常, 0: 已删除
  
  // 时间戳
  createdAt: string;
  updatedAt: string;
}
```

### 话题对象 (Topic)

```typescript
interface Topic {
  id: number;                    // 唯一 ID
  name: string;                  // 话题名称，不包含 #（如"苗寨风光"）
  description?: string;          // 话题描述
  icon?: string;                 // 话题图标（emoji 或 URL）
  
  // 统计数据
  postCount: number;             // 该话题下的游记数
  followerCount: number;         // 关注该话题的用户数
  isFollowing?: boolean;         // 当前用户是否已关注
  
  // 状态
  isRecommend: boolean;          // 是否为推荐话题
  
  // 时间戳
  createdAt: string;
  updatedAt: string;
}
```

### 用户对象 (User)

```typescript
interface User {
  id: number;                    // 唯一 ID
  nickname: string;              // 昵称
  avatar?: string;               // 头像 URL
  bio?: string;                  // 个人简介（≤ 500 字）
  region?: string;               // 地区
  
  // 统计数据
  followerCount: number;         // 粉丝数
  followingCount: number;        // 关注数
  likeCount: number;             // 获赞数（所有游记被点赞的总次数）
  postCount: number;             // 发布的游记数
  
  // 关系
  isFollowing?: boolean;         // 当前用户是否已关注该用户
  isFollowedBy?: boolean;        // 该用户是否已关注当前用户
  
  // 时间戳
  createdAt: string;
  updatedAt: string;
}
```

---

## 🔌 API 接口设计

### 基础约定

**基础 URL**: `http://localhost:5173/api`

**认证方式**: 
```http
Authorization: Bearer {jwt_token}
```

**响应格式**:
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误处理**:
- `code !== 0` 时，前端显示 `message` 错误提示
- `code = 1001 或 1002` 时（认证失败），跳转到登录页

---

### 游记接口

#### GET `/community/posts` - 获取游记列表

**查询参数**:
```
GET /community/posts?tab=hot&topicId=1&page=1&pageSize=10&sortBy=latest
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| tab | string | 否 | 筛选方式：all / hot / following（默认 all） |
| topicId | number | 否 | 话题 ID，指定则只显示该话题下的游记 |
| page | number | 否 | 页码（默认 1） |
| pageSize | number | 否 | 每页数量（默认 10，最大 100） |
| sortBy | string | 否 | 排序方式：latest / hot / likes / comments（默认 latest） |
| startDate | string | 否 | 开始日期（ISO 8601 格式） |
| endDate | string | 否 | 结束日期（ISO 8601 格式） |

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "title": "乌东苗寨梯田风光",
        "content": "...",
        "images": "[\"url1\", \"url2\"]",
        "location": "乌东村",
        "topicIds": [1, 2],
        "userId": 2,
        "author": {
          "id": 2,
          "nickname": "苗岭行者",
          "avatar": "http://..."
        },
        "viewCount": 1230,
        "likeCount": 89,
        "commentCount": 12,
        "favoriteCount": 34,
        "isHot": true,
        "isLiked": false,
        "isFavorited": true,
        "createdAt": "2026-09-10T10:30:00Z",
        "publishedAt": "2026-09-10T12:00:00Z",
        "status": 1
      }
    ],
    "total": 256,
    "page": 1,
    "pageSize": 10
  }
}
```

---

#### GET `/community/posts/:id` - 获取游记详情

**请求**:
```
GET /community/posts/1
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "title": "乌东苗寨梯田风光",
    "content": "详细的游记内容...",
    "images": "[\"url1\", \"url2\", \"url3\"]",
    "videoUrl": "https://...",
    "location": "乌东村",
    "topics": [
      {
        "id": 1,
        "name": "苗寨风光",
        "description": "..."
      }
    ],
    "userId": 2,
    "author": {
      "id": 2,
      "nickname": "苗岭行者",
      "avatar": "http://...",
      "bio": "热爱苗寨风光的摄影爱好者",
      "region": "贵州贵阳"
    },
    "viewCount": 1230,
    "likeCount": 89,
    "commentCount": 12,
    "favoriteCount": 34,
    "isHot": true,
    "isLiked": false,
    "isFavorited": true,
    "createdAt": "2026-09-10T10:30:00Z",
    "publishedAt": "2026-09-10T12:00:00Z",
    "status": 1
  }
}
```

---

#### POST `/community/posts` - 发布游记

**权限**：需要登录（`@Auth()`）

**请求体**:
```json
{
  "title": "乌东苗寨梯田风光",
  "content": "详细的游记内容...",
  "images": "[\"url1\", \"url2\"]",
  "videoUrl": "https://...",
  "location": "乌东村",
  "topicId": 1,
  "topicIds": [1, 2]
}
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 999,
    "title": "...",
    "status": 0,
    "message": "发布成功，已提交审核"
  }
}
```

---

### 评论接口

#### GET `/community/posts/:postId/comments` - 获取评论列表

**查询参数**:
```
GET /community/posts/1/comments?page=1&pageSize=10&sortBy=latest
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码（默认 1） |
| pageSize | number | 否 | 每页数量（默认 10） |
| sortBy | string | 否 | 排序方式：latest / likes（默认 latest） |

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "postId": 1,
        "userId": 3,
        "content": "写得真好，景色太美了！",
        "parentId": 0,
        "author": {
          "id": 3,
          "nickname": "银铃叮当",
          "avatar": "http://..."
        },
        "likeCount": 5,
        "isLiked": false,
        "status": 1,
        "createdAt": "2026-09-10T14:30:00Z",
        "replies": [
          {
            "id": 2,
            "postId": 1,
            "userId": 4,
            "content": "@银铃叮当 谢谢！确实很值得去",
            "parentId": 1,
            "replyUserId": 3,
            "replyUser": { "id": 3, "nickname": "银铃叮当" },
            "author": {
              "id": 4,
              "nickname": "行摄黔东南",
              "avatar": "http://..."
            },
            "likeCount": 2,
            "isLiked": false,
            "status": 1,
            "createdAt": "2026-09-10T16:00:00Z"
          }
        ]
      }
    ],
    "total": 12,
    "page": 1,
    "pageSize": 10
  }
}
```

---

#### POST `/community/posts/:postId/comments` - 发布评论

**权限**：需要登录（`@Auth()`）

**请求体**:
```json
{
  "content": "写得真好，景色太美了！",
  "parentId": 0,
  "replyUserId": null
}
```

或（回复评论）:
```json
{
  "content": "@银铃叮当 谢谢！确实很值得去",
  "parentId": 1,
  "replyUserId": 3
}
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 999,
    "postId": 1,
    "userId": 2,
    "content": "...",
    "parentId": 0,
    "author": { ... },
    "likeCount": 0,
    "status": 1,
    "createdAt": "2026-09-11T10:00:00Z"
  }
}
```

---

#### POST `/community/comments/:commentId/like` - 评论点赞

**权限**：需要登录（`@Auth()`）

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "liked": true,
    "likeCount": 6
  }
}
```

---

#### DELETE `/community/comments/:commentId` - 删除评论

**权限**：需要登录，且是评论作者或游记作者（`@Auth()`）

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

---

### 互动接口

#### POST `/community/posts/:postId/like` - 点赞游记

**权限**：需要登录（`@Auth()`）

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "liked": true,
    "likeCount": 90
  }
}
```

---

#### POST `/community/posts/:postId/favorite` - 收藏游记

**权限**：需要登录（`@Auth()`）

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "favorited": true,
    "favoriteCount": 35
  }
}
```

---

#### POST `/community/posts/:postId/report` - 举报游记

**权限**：需要登录（`@Auth()`）

**请求体**:
```json
{
  "reason": "虚假内容",
  "description": "这个地点的照片与实际不符"
}
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "reportId": 999,
    "message": "已提交举报，感谢您的反馈"
  }
}
```

---

### 话题接口

#### GET `/community/topics` - 获取话题列表

**查询参数**:
```
GET /community/topics?search=苗&sortBy=hot&page=1&pageSize=10
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| search | string | 否 | 搜索关键词 |
| sortBy | string | 否 | 排序方式：latest / hot / followers（默认 hot） |
| page | number | 否 | 页码（默认 1） |
| pageSize | number | 否 | 每页数量（默认 10） |

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "name": "苗寨风光",
        "description": "分享乌东苗寨的梯田、吊脚楼与云海",
        "icon": "🏞️",
        "postCount": 256,
        "followerCount": 1234,
        "isFollowing": false,
        "isRecommend": true,
        "createdAt": "2026-09-01T10:00:00Z"
      }
    ],
    "total": 4,
    "page": 1,
    "pageSize": 10
  }
}
```

---

#### POST `/community/topics/:topicId/follow` - 关注话题

**权限**：需要登录（`@Auth()`）

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "following": true,
    "followerCount": 1235
  }
}
```

---

#### POST `/community/topics/:topicId/unfollow` - 取消关注话题

**权限**：需要登录（`@Auth()`）

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "following": false,
    "followerCount": 1234
  }
}
```

---

### 用户接口

#### GET `/user/:userId` - 获取用户信息

**请求**:
```
GET /user/2
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 2,
    "nickname": "苗岭行者",
    "avatar": "http://...",
    "bio": "热爱苗寨风光的摄影爱好者",
    "region": "贵州贵阳",
    "followerCount": 234,
    "followingCount": 89,
    "likeCount": 1230,
    "postCount": 45,
    "isFollowing": false,
    "createdAt": "2026-09-01T10:00:00Z"
  }
}
```

---

#### GET `/user/:userId/posts` - 获取用户发布的游记

```
GET /user/2/posts?page=1&pageSize=12
```

响应与 `/community/posts` 相同。

---

#### GET `/user/:userId/likes` - 获取用户点赞的游记

```
GET /user/2/likes?page=1&pageSize=12
```

响应与 `/community/posts` 相同。

---

#### GET `/user/:userId/favorites` - 获取用户收藏的游记

```
GET /user/2/favorites?page=1&pageSize=12
```

响应与 `/community/posts` 相同。

---

#### GET `/user/:userId/following` - 获取用户关注的作者

```
GET /user/2/following?page=1&pageSize=12
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 3,
        "nickname": "银铃叮当",
        "avatar": "http://...",
        "bio": "非遗手工艺收藏者",
        "followerCount": 123,
        "followingCount": 45,
        "isFollowing": true
      }
    ],
    "total": 89,
    "page": 1,
    "pageSize": 12
  }
}
```

---

#### GET `/user/:userId/followingTopics` - 获取用户关注的话题

```
GET /user/2/followingTopics?page=1&pageSize=12
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "name": "苗寨风光",
        "description": "...",
        "postCount": 256,
        "followerCount": 1234,
        "isFollowing": true
      }
    ],
    "total": 4,
    "page": 1,
    "pageSize": 12
  }
}
```

---

#### POST `/user/:userId/follow` - 关注用户

**权限**：需要登录（`@Auth()`）

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "following": true,
    "followerCount": 235
  }
}
```

---

### 搜索接口

#### GET `/community/search` - 全局搜索

**查询参数**:
```
GET /community/search?keyword=苗寨&type=all&sortBy=latest&page=1&pageSize=10
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| keyword | string | 是 | 搜索关键词 |
| type | string | 否 | 搜索类型：all / posts / topics / users（默认 all） |
| sortBy | string | 否 | 排序方式：latest / hot / likes（默认 latest） |
| startDate | string | 否 | 开始日期 |
| endDate | string | 否 | 结束日期 |
| page | number | 否 | 页码（默认 1） |
| pageSize | number | 否 | 每页数量（默认 10） |

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "posts": [
      { /* Post 对象 */ }
    ],
    "topics": [
      { /* Topic 对象 */ }
    ],
    "users": [
      { /* User 对象 */ }
    ],
    "total": 256,
    "searchTime": 45
  }
}
```

---

### 文件上传接口

#### POST `/upload/file` - 上传图片

**权限**：需要登录（`@Auth()`）

**请求**:
```
POST /upload/file
Content-Type: multipart/form-data

file: <binary image data>
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "url": "http://localhost:5173/uploads/2026/09/abc123.jpg",
      "filename": "abc123.jpg",
      "size": 245120,
      "width": 1920,
      "height": 1080
    }
  ]
}
```

---

## 📱 页面流程图

### 用户浏览游记流程

```
社区首页 (/community)
    ↓
查看热门话题 / 热门游记
    ↓
点击游记卡片
    ↓
进入游记详情页 (/community/:id)
    ├─ 查看完整内容、图片、视频
    ├─ 点赞 / 收藏游记
    ├─ 查看评论
    └─ 发表评论（需登录）
    ↓
点击作者名
    ↓
进入用户主页 (/user/:userId)
    ├─ 查看用户发布的游记
    ├─ 查看用户点赞的游记
    ├─ 查看用户收藏的游记
    ├─ 查看用户关注的作者
    ├─ 查看用户关注的话题
    └─ 点击关注用户
```

### 用户发布游记流程

```
登录 (/login)
    ↓
进入发布页面 (/community/publish)
    ├─ 输入标题、正文
    ├─ 上传图片（1-9 张）
    ├─ 选择话题（可多选）
    ├─ 输入位置信息
    └─ 输入视频链接（可选）
    ↓
点击发布
    ↓
后端内容审核
    ↓
【审核通过】
    ↓
游记发布，显示在游记列表
    ↓
用户可在主页查看该游记

【审核不通过】
    ↓
显示驳回原因
    ↓
用户可编辑重新发布
```

### 用户发表评论流程

```
进入游记详情页 (/community/:id)
    ↓
点击评论输入框
    ↓
【未登录】→ 重定向到登录页 (/login)
【已登录】→ 继续
    ↓
输入评论内容（1-500 字）
    ↓
点击发送
    ↓
评论立即显示在列表顶部
    ↓
用户可点赞评论 / 点击回复 / 删除自己的评论
```

### 用户浏览话题流程

```
社区首页 (/community)
    ↓
点击热门话题卡片
    ↓
进入话题详情页 (/community/feed?topic=:topicId)
    ├─ 显示话题信息
    ├─ 关注/取消关注话题
    └─ 显示该话题下的所有游记
    ↓
或点击"热门话题"链接
    ↓
进入话题列表页 (/community/topics)
    ├─ 搜索话题
    ├─ 按热度/关注数排序
    └─ 关注/取消关注话题
```

---

## 🎨 UI/UX 设计原则

### 色彩方案

| 用途 | 颜色 | HEX 值 |
|------|------|--------|
| 主色 | 蓝色 | #409EFF |
| 成功 | 绿色 | #67C23A |
| 警告 | 橙色 | #E6A23C |
| 危险 | 红色 | #F56C6C |
| 文字 | 深灰 | #333333 |
| 次文字 | 中灰 | #666666 |
| 辅文字 | 浅灰 | #999999 |
| 背景 | 极浅灰 | #F5F5F5 |
| 分割线 | 浅灰 | #EEEEEE |

### 字体规范

| 用途 | 大小 | 权重 |
|------|------|------|
| 大标题 | 28px | bold |
| 页面标题 | 24px | bold |
| 卡片标题 | 18px | bold |
| 正文 | 14px | normal |
| 辅文字 | 12px | normal |
| 小标签 | 11px | normal |

### 间距规范

| 用途 | 大小 |
|------|------|
| 页面外边距 | 20px |
| 卡片内边距 | 16px |
| 卡片间距 | 16px |
| 行高间距 | 8px |
| 按钮间距 | 12px |

---

## 🧪 测试场景

### 功能测试清单

#### 游记浏览测试
- [ ] 进入社区首页，热门话题和游记正常加载
- [ ] 游记列表按话题过滤正确
- [ ] 游记列表排序（最新/热门）正确
- [ ] 点击游记进入详情页正常
- [ ] 游记详情页显示所有内容（标题、作者、图片、评论等）
- [ ] 点击作者名进入用户主页正确

#### 评论功能测试
- [ ] 登录用户可发表评论
- [ ] 未登录用户点击评论框提示登录
- [ ] 评论内容验证（1-500 字）
- [ ] 一级评论和二级回复正确展示
- [ ] 评论点赞功能正常（状态切换、计数更新）
- [ ] 评论删除权限检查（只有评论者和游记作者可删除）
- [ ] 评论列表分页正常加载

#### 发布游记测试
- [ ] 未登录用户进入发布页重定向到登录
- [ ] 标题、正文字数限制检查
- [ ] 图片上传验证（格式、大小、数量）
- [ ] 图片预览和删除功能
- [ ] 话题多选功能
- [ ] 位置信息输入和候选展示
- [ ] 发布成功提示和重定向
- [ ] 发布后游记状态为"审核中"

#### 话题功能测试
- [ ] 热门话题列表加载正常
- [ ] 话题搜索功能
- [ ] 话题排序（最新/热度/关注数）
- [ ] 关注/取消关注话题功能
- [ ] 点击话题进入详情页，显示该话题下的游记
- [ ] 话题统计数据更新正确

#### 用户主页测试
- [ ] 用户信息显示正确
- [ ] 五个标签页正常切换
- [ ] "发布的游记"标签页内容展示
- [ ] "点赞的游记"标签页内容展示（隐私权限检查）
- [ ] "收藏的游记"标签页内容展示（隐私权限检查）
- [ ] "关注的作者"标签页内容展示
- [ ] "关注的话题"标签页内容展示
- [ ] 关注/取消关注用户功能

#### 互动功能测试
- [ ] 点赞游记功能（状态切换、计数更新）
- [ ] 收藏游记功能（状态切换、计数更新）
- [ ] 举报游记功能（表单提交、确认提示）
- [ ] 分享游记功能

#### 搜索功能测试
- [ ] 全局搜索正常进行
- [ ] 搜索结果按类型分类显示
- [ ] 搜索排序选项
- [ ] 日期范围筛选
- [ ] 搜索结果分页

---

## 📊 数据初始化

### 话题数据初始化 SQL

```sql
-- 插入乌东文旅相关话题
INSERT INTO t_topic (name, description, icon, is_recommend, created_at) VALUES
('苗寨风光', '分享乌东苗寨的梯田、吊脚楼与云海', '🏞️', 1, NOW()),
('非遗手作', '银饰、蜡染、刺绣等非遗体验记录', '🎨', 1, NOW()),
('苗家美食', '长桌宴、酸汤鱼、米酒等美食分享', '🍽️', 0, NOW()),
('旅拍攻略', '乌东村摄影机位与游玩攻略', '📷', 0, NOW());
```

---

## 🚀 性能优化建议

1. **图片优化**：使用 WebP 格式，根据设备分辨率加载不同尺寸
2. **列表虚拟化**：游记列表超过 100 条时使用虚拟滚动
3. **缓存策略**：热门话题和推荐游记使用浏览器缓存（1 小时）
4. **预加载**：用户向下滚动时预加载下一页内容
5. **CDN 分发**：上传的图片通过 CDN 加速分发
6. **数据库索引**：在 user_id、topic_id、created_at 字段建立索引

---

## 📝 更新日志

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-09-11 | 初版文档，包含所有功能需求、API 设计、数据结构 |

---

